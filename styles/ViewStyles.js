import { StyleSheet } from 'react-native'

export const ViewStyles = StyleSheet.create({
    safeAreaStyle: {
        backgroundColor: 'black',
        flex: 1,
        alignItems: 'center',
    },
})